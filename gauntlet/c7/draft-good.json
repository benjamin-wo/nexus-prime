{
  "id": "budget",
  "description": "Track a trip budget: set a target, add planned costs, and compare actual spending against it. Ask like 'is my Japan trip budget at risk?'.",
  "input_schema": {"type": "object", "properties": {"prompt": {"type": "string"}}, "required": ["prompt"]},
  "output_schema": {"type": "object", "properties": {"message": {"type": "string"}}, "required": ["message"]},
  "side_effect": "read",
  "tags": ["budget", "finance"],
  "managers": ["finance"],
  "preconditions": ["Expenses capability is registered"],
  "cost_hint": "low",
  "requires_human": false,
  "source_gap_id": "c3-probe1-budget"
}
